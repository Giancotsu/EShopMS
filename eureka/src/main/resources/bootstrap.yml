# ==============================================================
# = Eureka Server
# ==============================================================

spring:
  cloud:
    config:
      name: EshopEureka
      profile: peer1
      discovery:
        enabled: true
        service-id: EshopConfigServer
      enabled: true